# Copy this file to scripts/.env and fill in real values.
# scripts/.env is gitignored (see .gitignore) — it will never be committed.
SUPERUSER_EMAIL=admin@example.com
SUPERUSER_PASSWORD=your-password-here
