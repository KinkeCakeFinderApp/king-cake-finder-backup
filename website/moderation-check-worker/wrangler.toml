name = "king-cake-moderation-check"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Workers AI (Llama Guard 3) — no API key needed at all; this binding
# authenticates automatically as part of the Cloudflare account, and it's on
# the same free tier (10,000 neurons/day, no card) as the worker itself.
[ai]
binding = "AI"
